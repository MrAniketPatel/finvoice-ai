# MongoDB Connection String
# Get this from MongoDB Atlas: https://cloud.mongodb.com/
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/FinVoiceBeta

# JWT Secret Key
# Generate a secure random string (64+ characters recommended)
# You can generate one using: openssl rand -hex 64
JWT_SECRET=your-super-secure-random-jwt-secret-key-here-minimum-32-characters

# Server Port
PORT=5000

# Node Environment (development or production)
NODE_ENV=production
